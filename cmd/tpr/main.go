package main

func main() {
	/*
		options := flags.Parse()

		deps := &types.RunnerDependencies{
			C:  cli.New(),
			T:  testflinger.New(),
			Sd: systemd.New(),
			Sp: splitter.New(),
		}
		runner := runner.New(deps)

		if err := runner.Run(options); err != nil {
			log.Fatalf(err)
		}
	*/
}
